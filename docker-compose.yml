version: '3.8'

services:
  backend:
    build: ./backend
    container_name: ai-plc-backend
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///app/data/ai_plc.db
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./frontend
    container_name: ai-plc-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: always
